name: Main pipeline

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js v18
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - run: npm install
      - run: npx nx run-many --target=build --all=true --skip-nx-cache

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js v18
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - run: npm install
      - run: npx nx run-many --target=lint --all=true --skip-nx-cache
